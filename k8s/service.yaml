apiVersion: v1
kind: Service
metadata:
  name: java-band-service
  namespace: default
  labels:
    app: spring-k8-demo     # 🔴 REQUIRED
spec:
  type: NodePort
  selector:
    app: spring-k8-demo
  ports:
    - name: http            # 🔴 REQUIRED for ServiceMonitor
      port: 80
      targetPort: 8088
